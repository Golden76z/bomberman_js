import { playerInfos } from './entities/player_infos.js'

// Function that check if a movement can be done (doesn't override wall)
export function canMoveHorizontaly(position) {

}

// Function that check if a movement can be done verticaly
export function canMoveVerticaly(position) {

}
